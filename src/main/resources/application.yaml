server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mystore?serverTimeZone=ADT
    username: postgres
    password: postdba
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true